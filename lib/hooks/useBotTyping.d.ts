import { ChatEntry } from "../../types";
export declare const useBotTyping: (content: ChatEntry[], setContent: (content: ChatEntry[]) => void, lastInputValue: string, avatarString?: string | undefined) => boolean;
